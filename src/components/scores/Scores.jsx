import React from 'react';
import './scores/Scores.scss'; // Import SCSS file

const Scores = () => {
  return (
    <div className="scores-container">
      <h2>Scores</h2>
      <p>This is the scores area.</p>
    </div>
  );
};

export default Scores;
